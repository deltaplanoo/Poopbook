// PoopBook is a pure client-side SPA/PWA: PocketBase auth lives in
// localStorage and every view talks to the API directly from the browser.
export const ssr = false;
export const prerender = false;
